context:
  name: mednet
  version: "2.1.0"
  python_min: "3.12"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://files.pythonhosted.org/packages/fa/7e/c28a6d325d9bb04a20af4db2e7d55e38d8c31826275349b932a7b801ad1b/mednet-${{ version }}.tar.gz
  sha256: 3e5a71514e4bfebf4855ecfe773b67695bf210747a214966e5b650c9a9ae5e07

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  python:
    entry_points:
      - mednet = mednet.scripts.cli:cli

requirements:
  host:
    - python ${{ python_min }}.*
    - hatchling
    - versioningit
    - pip
  run:
    - python >=${{ python_min }}
    - clapper >=1.3.4
    - click
    - colorlog
    - compact-json
    - credible >=1.0.2
    - fairret >=0.1.3,<0.2
    - grad-cam >=1.5.0
    - lightning >=2.2.0
    - loky
    - h5py
    - matplotlib-base
    - mlflow-skinny
    - numpy
    - peft  # needs transformers
    - transformers >=4.36.0  # avoids huggingface-hub conflicts
    - pillow
    - psutil
    - python-gitlab
    - scikit-image
    - scikit-learn
    - scipy
    - tabulate
    - tensorboard
    - timm
    - torchvision >=0.18.0
    - pytorch
    - torchio
    - torchmetrics
    - tqdm
    - versioningit
tests:
  - python:
      imports:
        - mednet
      pip_check: true
      python_version: ${{ python_min }}.*
  - files:
      source:
        - tests
    requirements:
      run:
        - pip
        - pytest
    script:
      - mednet --help
      - pytest --override-ini pythonpath=tests/src -sv tests/cli/test_cli.py

about:
  summary: Multi-task Library to Develop Computer-Aided Tools for Medical Data Analysis
  license: GPL-3.0-or-later
  license_file: LICENSES/GPL-3.0-or-later.txt
  homepage: https://gitlab.idiap.ch/medai/software/mednet
  repository: https://gitlab.idiap.ch/medai/software/mednet
  documentation: https://mednet.readthedocs.io

extra:
  recipe-maintainers:
    - anjos
