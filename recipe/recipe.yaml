context:
  name: mednet
  version: "2.0.1"
  python_min: 3.11

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://files.pythonhosted.org/packages/4a/12/2620f60b4a4250ad2082701b12470ed07c15dbafceaaa3dfb8f96a31f993/mednet-${{ version }}.tar.gz
  sha256: ae7ae16f256b12209eca132e8cc195b4195530e9e789fbbc1398b61207f41e01

build:
  number: 2
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  python:
    entry_points:
      - mednet = mednet.scripts.cli:cli

requirements:
  host:
    - python ${{ python_min }}.*
    - hatchling
    - versioningit
    - pip
  run:
    - python >=${{ python_min }}
    - clapper >=1.3.1
    - click
    - colorlog
    - compact-json
    - credible >=1.0.2
    - grad-cam >=1.5.0
    - lightning >=2.2.0
    - loky
    - h5py
    - matplotlib-base
    - mlflow-skinny
    - numpy
    - peft
    - pillow
    - psutil
    - python-gitlab
    - scikit-image
    - scikit-learn
    - scipy
    - tabulate
    - tensorboard
    - timm
    - torchvision >=0.18.0
    - pytorch
    - torchio
    - torchmetrics
    - tqdm
    - versioningit

tests:
  - python:
      imports:
        - mednet
      pip_check: true
      python_version: ${{ python_min }}.*
  - files:
      source:
        - tests
    requirements:
      run:
        - pip
        - pytest
    script:
      - mednet --help
      - pytest --override-ini pythonpath=tests/src -sv tests/cli/test_cli.py

about:
  summary: Multi-task Library to Develop Computer-Aided Tools for Medical Data Analysis
  license: GPL-3.0-or-later
  license_file: LICENSES/GPL-3.0-or-later.txt
  homepage: https://gitlab.idiap.ch/medai/software/mednet
  repository: https://gitlab.idiap.ch/medai/software/mednet
  documentation: https://mednet.readthedocs.io

extra:
  recipe-maintainers:
    - anjos
